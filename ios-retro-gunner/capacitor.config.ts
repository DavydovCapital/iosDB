import { CapacitorConfig } from "@capacitor/cli";

const config: CapacitorConfig = {
  appId: "com.cosmokramerii.retrogunner",
  appName: "Cobra Strike",
  webDir: "dist",
  ios: { scheme: "capacitor" },
};

export default config;
